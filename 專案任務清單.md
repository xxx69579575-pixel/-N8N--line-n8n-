# 本地AI企業問答助理 — 最終專案任務清單

> 更新日期：2026-03-24
> 架構：n8n + Qwen2.5 (Ollama) + PostgreSQL + pgvector + LINE Messaging API

---

## 整體架構

```
企業文件 (Word/PDF/Excel/Image)
    └── n8n 文件匯入流程
        └── OCR / 文字抽取 / 切片 / Embedding
            └── PostgreSQL + pgvector（知識庫）
                └── n8n 問答流程
                    ├── LINE 使用者提問
                    └── Qwen2.5 生成回答 → LINE 回覆
```

---

## Phase 0 — 基礎環境建置

| 狀態 | 任務 | 說明 | 優先 |
|------|------|------|------|
| ⬜ | **0-1 修正 schema 向量維度衝突** | `init.sql` 為 `VECTOR(768)`，`02_postgresql_schema.sql` 為 `VECTOR(1024)`，需確認使用哪個 embedding 模型並統一維度後再部署 | 🔴 必做 |
| ⬜ | **0-2 啟動 Docker PostgreSQL + pgvector** | 進入 `docker_postgreSQL/`，執行 `docker compose up -d`，確認 pg 容器與 pgAdmin 正常運作 | 🔴 必做 |
| ⬜ | **0-3 執行完整 schema 建立** | 對 `vectordb` 資料庫執行 `02_postgresql_schema.sql`，建立 5 張核心表與索引、trigger | 🔴 必做 |
| ⬜ | **0-4 補建 qa_logs 資料表** | `02_postgresql_schema.sql` 缺少問答日誌表，需補充建立（欄位：id, user_id, question, retrieved_chunk_ids, answer, confidence, created_at） | 🔴 必做 |
| ⬜ | **0-5 補建 conversation_sessions 資料表** | 工作流設計要求保留 1~3 輪對話記憶，需建立 sessions 表（欄位：id, line_user_id, turns JSONB, updated_at） | 🔴 必做 |
| ⬜ | **0-6 安裝 Ollama 並下載模型** | 安裝 Ollama，拉取 Qwen2.5 chat 模型與選定的 embedding 模型（nomic-embed-text 768 維 / bge-m3 1024 維擇一） | 🔴 必做 |
| ⬜ | **0-7 設定 LINE Webhook 公開網址** | n8n 在本機無法直接接收 LINE Webhook，需設定 ngrok 或反向代理以取得 HTTPS 公開 URL，並填入 LINE Developer Console | 🔴 必做 |
| ⬜ | **0-8 建立 .env.example 與本地 .env** | 依兩份規格書整合所有環境變數，建立範本檔，含 ingest、postgres、ollama、line、qa 設定 | 🔴 必做 |
| ⬜ | **0-9 建立專案資料夾結構** | 建立 `workflows/`、`scripts/`、`sql/`、`config/`、`docs/`，以及 inbox/processing/processed/error 四個檔案夾 | 🟡 建議 |

---

## Phase 1 — 文件匯入流程（n8n Ingest Workflow）

| 狀態 | 任務 | 說明 | 優先 |
|------|------|------|------|
| ⬜ | **1-1 建立 Schedule Trigger 掃描節點** | 每 1~5 分鐘掃描 inbox，過濾支援副檔名（.docx/.pdf/.xlsx/.xls/.jpg/.jpeg/.png），掃出清單後逐檔處理 | 🔴 必做 |
| ⬜ | **1-2 移檔至 processing 資料夾** | 掃到檔案後立即移至 processing，防止重複觸發與並發衝突 | 🔴 必做 |
| ⬜ | **1-3 計算 SHA-256 hash 去重** | 對每個檔案計算 hash，查詢 documents 表；已存在則寫 log 後跳過，不存在則繼續 | 🔴 必做 |
| ⬜ | **1-4 擷取 metadata** | 收集 file_name、file_ext、file_path、mime_type、file_size、source_folder、created_time、modified_time | 🔴 必做 |
| ⬜ | **1-5 Word 文字抽取節點** | 抽取 .docx 正文與表格文字，保留 section_title | 🔴 必做 |
| ⬜ | **1-6 PDF 文字抽取 + 掃描判斷節點** | 先嘗試直接抽文字；若結果接近空值，判定為掃描型 PDF，導向 OCR 分支 | 🔴 必做 |
| ⬜ | **1-7 Excel 文字化節點** | 讀取所有工作表，將每列轉為可檢索文字，保留 sheet_name | 🔴 必做 |
| ⬜ | **1-8 OCR 節點（圖片與掃描 PDF）** | 對圖片與掃描 PDF 呼叫 OCR 服務，取得文字；無法識別時記錄 parse_status | 🔴 必做 |
| ⬜ | **1-9 文字清洗節點** | 去除多餘空白、合併異常換行、去除控制字元、標準化全形半形 | 🔴 必做 |
| ⬜ | **1-10 切片 chunking 節點** | 每片 500~1000 字元，overlap 100~200 字元；保留 chunk_index、page_no/sheet_name/section_title | 🔴 必做 |
| ⬜ | **1-11 Embedding 向量化節點** | 對每個 chunk 呼叫 Ollama embedding API，取得向量後準備寫入 | 🔴 必做 |
| ⬜ | **1-12 寫入 PostgreSQL（5 張表）** | 依序寫入：documents → document_contents → document_chunks → document_permissions → processing_logs | 🔴 必做 |
| ⬜ | **1-13 搬檔節點（成功/失敗）** | 成功移至 processed；失敗移至 error，寫入 processing_logs 含錯誤訊息 | 🔴 必做 |
| ⬜ | **1-14 錯誤隔離機制** | 單檔失敗不中斷整批，每個錯誤分支都要有 log 寫入與 error 資料夾移檔 | 🔴 必做 |

---

## Phase 2 — 企業問答流程（n8n QA Workflow）

| 狀態 | 任務 | 說明 | 優先 |
|------|------|------|------|
| ⬜ | **2-1 LINE Webhook 接收節點** | 建立 n8n webhook，接收 LINE 事件；解析 userId、replyToken、messageText、timestamp | 🔴 必做 |
| ⬜ | **2-2 LINE 簽章驗證** | 使用 LINE_CHANNEL_SECRET 驗證 X-Line-Signature header，拒絕偽造請求 | 🔴 必做 |
| ⬜ | **2-3 非文字訊息處理** | 若不是 text message，回覆「目前僅支援文字提問」 | 🟡 建議 |
| ⬜ | **2-4 使用者權限驗證節點** | 根據 userId 查詢是否為授權使用者；未授權回覆「無查詢權限」 | 🔴 必做 |
| ⬜ | **2-5 取得對話記憶節點** | 查詢 conversation_sessions，取出該使用者最近 1~3 輪歷史 | 🟡 建議 |
| ⬜ | **2-6 Query Embedding 節點** | 將使用者問題送 Ollama embedding 模型，產生 query vector | 🔴 必做 |
| ⬜ | **2-7 pgvector 相似度檢索節點** | 查詢 document_chunks，含相似度排序、最低門檻（QA_MIN_SIMILARITY）、權限過濾 | 🔴 必做 |
| ⬜ | **2-8 查無資料處理** | 若檢索結果低於門檻，回覆「目前知識庫無足夠資訊，請換句話提問或洽管理者」 | 🔴 必做 |
| ⬜ | **2-9 Prompt 組裝節點** | 整合問題 + chunk 內容 + 對話歷史 + 回答規則（只能根據提供內容、不得捏造、不足時明說） | 🔴 必做 |
| ⬜ | **2-10 Qwen2.5 回答生成節點** | 呼叫本地 Ollama Qwen2.5，生成答案；格式建議含 answer、confidence、source_refs | 🔴 必做 |
| ⬜ | **2-11 LINE 回覆節點** | 使用 replyToken 回傳答案；長度超出限制時截斷或分段 | 🔴 必做 |
| ⬜ | **2-12 更新對話記憶節點** | 將本輪問答寫回 conversation_sessions，超過 3 輪時移除最舊一輪 | 🟡 建議 |
| ⬜ | **2-13 寫入 qa_logs** | 記錄 userId、question、retrieved_chunk_ids、answer、confidence、created_at | 🔴 必做 |
| ⬜ | **2-14 失敗保底回覆** | 任何節點失敗時，至少回覆「系統暫時無法回應，請稍後再試」，不能靜默失敗 | 🔴 必做 |

---

## Phase 3 — 進階功能（系統品質提升）

| 狀態 | 任務 | 說明 | 優先 |
|------|------|------|------|
| ⬜ | **3-1 Hybrid Search（向量 + BM25 全文）** | PostgreSQL 原生支援 `tsvector`，結合向量搜尋與關鍵字搜尋後用 RRF 重排，對精確名詞（人名、型號、法條）效果顯著提升 | 🟡 建議 |
| ⬜ | **3-2 LINE Flex Message 結構化回覆** | 將純文字回覆升級為 Flex Message 卡片：答案區塊 + 來源文件名稱 + 快速回覆按鈕（「換句話問」/ 「回報問題」） | 🟡 建議 |
| ⬜ | **3-3 使用者回饋機制（讚/倒讚）** | 回答後附上評分按鈕，收集 feedback 存入 qa_feedback 表，供後續分析與優化 | 🟡 建議 |
| ⬜ | **3-4 文件版本更新策略** | 當相同文件（依檔名或業務 ID）有新版本時，標記舊版 chunks 為 archived，匯入新版，避免問答系統同時看到新舊矛盾內容 | 🟡 建議 |
| ⬜ | **3-5 文件到期 / 下架機制** | 為 documents 表新增 expires_at 欄位，過期文件的 chunks 自動排除於問答檢索範圍 | 🟡 建議 |
| ⬜ | **3-6 Embedding 模型遷移計畫** | 若未來更換 embedding 模型（如從 768 維換至 1024 維），需要批次重新 embed 所有 chunks，建立對應的遷移腳本 | 🟡 建議 |
| ⬜ | **3-7 知識缺口分析報告** | 定期統計 qa_logs 中被標記「查無資料」的問題，匯出清單通知文管人員補充知識庫 | 🟢 增值 |
| ⬜ | **3-8 n8n Workflow 自動備份** | 設定定期將 n8n workflow 匯出 JSON 並備份，防止容器損毀時工作流完全消失 | 🟡 建議 |

---

## Phase 4 — 驗收測試

| 狀態 | 任務 | 說明 | 優先 |
|------|------|------|------|
| ⬜ | **4-1 文件匯入驗收：Word** | 放入 .docx，確認 documents / document_contents / document_chunks 均正確建立 | 🔴 必做 |
| ⬜ | **4-2 文件匯入驗收：文字型 PDF** | 放入可直接抽文字的 PDF，確認成功抽文與向量化 | 🔴 必做 |
| ⬜ | **4-3 文件匯入驗收：掃描型 PDF / 圖片** | 放入掃描 PDF 或圖片，確認走 OCR 分支並成功寫入 | 🔴 必做 |
| ⬜ | **4-4 文件匯入驗收：Excel** | 放入 .xlsx，確認 sheet_name 保留，chunk 完成 | 🔴 必做 |
| ⬜ | **4-5 去重驗收** | 重複放入同一檔案，確認不會新增重複 documents | 🔴 必做 |
| ⬜ | **4-6 錯誤隔離驗收** | 故意放入損毀檔案，確認移至 error 且其他檔案不受影響 | 🔴 必做 |
| ⬜ | **4-7 問答基本流程驗收** | 從 LINE 傳問題，確認依序完成：embedding → 向量檢索 → Qwen2.5 生成 → LINE 回覆 → QA log 寫入 | 🔴 必做 |
| ⬜ | **4-8 查無資料驗收** | 問一個知識庫中不存在的問題，確認系統回覆適當「查無資料」訊息而非亂答 | 🔴 必做 |
| ⬜ | **4-9 無權限使用者驗收** | 使用未授權的 LINE userId，確認系統回覆無權限訊息 | 🔴 必做 |
| ⬜ | **4-10 簽章驗證驗收** | 用錯誤簽章呼叫 webhook，確認被拒絕 | 🔴 必做 |

---

## 交付物清單

| 項目 | 說明 |
|------|------|
| `workflows/ingest_workflow.json` | n8n 文件匯入工作流匯出檔 |
| `workflows/qa_workflow.json` | n8n 問答工作流匯出檔 |
| `sql/02_postgresql_schema.sql` | 完整 schema（含統一維度修正、qa_logs、conversation_sessions） |
| `scripts/` | 文字抽取、OCR、chunking 輔助腳本 |
| `config/.env.example` | 所有環境變數範本 |
| `docs/deployment.md` | 部署說明（含 ngrok 設定） |
| `docs/testing.md` | 測試方式說明 |

---

## 重要技術決策待確認

| # | 決策事項 | 選項 | 影響 |
|---|---------|------|------|
| A | **Embedding 模型維度** | nomic-embed-text（768）或 bge-m3（1024）| 影響 schema、向量索引設定 |
| B | **LINE Webhook 公開方式** | ngrok / Cloudflare Tunnel / 固定 IP 反向代理 | 影響穩定性與安全性 |
| C | **OCR 服務選擇** | Tesseract 本地 / PaddleOCR / 雲端 OCR | 影響中文辨識品質 |
| D | **Excel 處理深度** | 基礎文字化（第一版）/ 語意感知轉換（後續）| 影響 Excel 問答品質 |

---

## 優先序建議

```
Phase 0（修正問題 + 環境）→ Phase 1（Ingest 主流程）→ Phase 2（QA 主流程）
→ Phase 4（驗收）→ Phase 3（進階功能，按需逐步補）
```

> 先讓 Phase 1 + Phase 2 的主流程跑通，再補進階功能，避免功能未收斂就開始優化細節。
