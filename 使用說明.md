# 本地 AI 企業問答助理 — 使用說明

## 系統架構概覽

```
LINE 使用者
    ↓ 提問 / 找檔案
n8n 工作流 (port 5681)
    ↓
api_server.py (port 8765)  ←→  PostgreSQL + pgvector (port 65432)
    ↓
Ollama / Qwen2.5 (port 11434)
    ↓
LINE 回覆
```

---

## 一、啟動系統

### 1. 啟動資料庫（PostgreSQL + pgAdmin）

```bash
cd docker_postgreSQL
docker compose up -d
```

| 服務 | 網址 | 帳號密碼 |
|------|------|---------|
| PostgreSQL | localhost:65432 | testuser / testpwd |
| pgAdmin | http://localhost:5050 | admin@admin.com / root |
| n8n | http://localhost:5681 | — |

> pgAdmin 連線 PostgreSQL 時，hostname 填 `db`（docker 內部名稱）

### 2. 啟動 API Server

```bash
cd "D:\n8n\CLAUDE 實做\本地AI企業問答助理"
python scripts/api_server.py
```

啟動後監聽 `http://localhost:8765`，提供以下端點：

| 端點 | 方法 | 功能 |
|------|------|------|
| /health | GET | 健康檢查 |
| /files/\<filename\> | GET | 下載原始檔案 |
| /line-verify | POST | LINE 簽章驗證 |
| /vector-search | POST | 向量語意搜尋 |
| /prompt-builder | POST | 組合 RAG Prompt |
| /search-files | POST | 依關鍵字搜尋檔案 |

### 3. 建立外部連線（LINE Webhook 需要 HTTPS）

```bash
# 方法一：Cloudflare Quick Tunnel（暫時，重啟後 URL 會變）
cloudflared tunnel --url http://localhost:8765

# 方法二：Named Tunnel（永久 URL，推薦正式環境）
cloudflared tunnel login
cloudflared tunnel create qa-api
```

> 取得 URL 後，更新 n8n 工作流中 `Build File Links Reply` 節點的 `PUBLIC_API_BASE_URL` 變數

---

## 二、LINE Bot 使用方式

### 問答功能（問問題）

直接在 LINE 輸入問題，或點選 **「💬 問問題」** 按鈕：

```
你：職安法第23條規定什麼？
Bot：根據文件內容，職安法第23條規定...（附來源）
```

### 找檔案功能

點選 **「🔍 找檔案」** 按鈕，依步驟操作：

```
1. 點選「🔍 找檔案」
2. Bot 詢問：請選擇查詢檔案類型
   → [PDF] [Word] [Excel] [JPG]
3. 點選檔案類型（例如：PDF）
4. Bot 詢問：請輸入要搜尋的 PDF 檔案關鍵字
5. 輸入關鍵字（例如：楊富段）
6. Bot 回覆：找到 N 筆，附下載連結
```

### Quick Reply 按鈕

每次 Bot 回覆後，下方會出現快速選單：
- **🔍 找檔案** — 進入檔案搜尋流程
- **💬 問問題** — 回到問答模式

---

## 三、匯入新文件

### 批次匯入資料夾

```bash
cd "D:\n8n\CLAUDE 實做\本地AI企業問答助理"
python scripts/batch_ingest.py --dir "D:/你的資料夾" --department general
```

**支援格式**：`.pdf` `.docx` `.xlsx` `.xls` `.jpg` `.jpeg` `.png`

> `.xls` 舊格式需先轉換成 `.xlsx`，JPG/PNG 需安裝 Tesseract OCR

**匯入流程**（每個檔案自動執行）：
1. 抽取文字（PDF/Word/Excel 直接解析，圖片用 OCR）
2. 文字切片（800 字 / chunk，150 字重疊）
3. 產生 Embedding（Ollama bge-m3，1024 維）
4. 寫入 PostgreSQL（documents + chunks + embedding）

### 指定部門標籤

```bash
# 依部門分類文件（影響問答時的篩選）
python scripts/batch_ingest.py --dir "D:/人事資料" --department hr
python scripts/batch_ingest.py --dir "D:/財務報表" --department finance
python scripts/batch_ingest.py --dir "D:/職安法規" --department safety
```

### 更新檔案路徑（移動檔案後執行）

```bash
python scripts/update_file_paths.py --dir "D:/你的資料夾"
# 加上 --dry-run 先預覽
python scripts/update_file_paths.py --dir "D:/你的資料夾" --dry-run
```

---

## 四、資料庫管理

### 查看目前文件數量

```bash
python -c "
import psycopg2
conn = psycopg2.connect(host='localhost', port=65432, dbname='vectordb', user='testuser', password='testpwd')
with conn.cursor() as cur:
    cur.execute('SELECT file_name, ingest_status FROM documents ORDER BY created_at')
    for r in cur.fetchall(): print(r)
    cur.execute('SELECT COUNT(*) FROM document_chunks')
    print('Total chunks:', cur.fetchone()[0])
conn.close()
"
```

### 清空所有文件（保留帳號設定）

```bash
python -c "
import psycopg2
conn = psycopg2.connect(host='localhost', port=65432, dbname='vectordb', user='testuser', password='testpwd')
with conn.cursor() as cur:
    cur.execute('TRUNCATE TABLE document_chunks, document_contents, document_permissions, processing_logs, qa_logs, documents, conversation_sessions CASCADE')
    conn.commit()
    print('清空完成（allowed_users 保留）')
conn.close()
"
```

> `allowed_users` 不會被清空，LINE 用戶授權不受影響

### 查看 LINE 授權用戶

```bash
python -c "
import psycopg2
conn = psycopg2.connect(host='localhost', port=65432, dbname='vectordb', user='testuser', password='testpwd')
with conn.cursor() as cur:
    cur.execute('SELECT line_user_id, display_name, created_at FROM allowed_users')
    for r in cur.fetchall(): print(r)
conn.close()
"
```

### 新增 LINE 授權用戶

```bash
python -c "
import psycopg2
conn = psycopg2.connect(host='localhost', port=65432, dbname='vectordb', user='testuser', password='testpwd')
with conn.cursor() as cur:
    cur.execute(\"INSERT INTO allowed_users (line_user_id, display_name) VALUES ('U你的LINE_USER_ID', '姓名')\")
    conn.commit()
    print('新增完成')
conn.close()
"
```

> LINE User ID 在 n8n 工作流的執行記錄中可以找到

---

## 五、環境變數設定

設定檔位置：`config/.env`

```env
# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=65432
POSTGRES_DB=vectordb
POSTGRES_USER=testuser
POSTGRES_PASSWORD=testpwd

# Ollama
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_CHAT_MODEL=qwen2.5
OLLAMA_EMBED_MODEL=bge-m3

# LINE
LINE_CHANNEL_ACCESS_TOKEN=你的Token
LINE_CHANNEL_SECRET=你的Secret

# 問答參數
QA_TOP_K=5
QA_MIN_SIMILARITY=0.7

# 檔案下載基礎路徑
FILES_BASE_DIR=D:/職安
```

---

## 六、常見問題

**Q：Bot 回覆「您無使用權限」**
A：LINE User ID 未加入 `allowed_users` 表，依上方說明新增即可

**Q：Bot 回覆「目前知識庫中找不到相關資料」**
A：問題與已匯入文件相關性不高，或尚未匯入相關文件

**Q：找檔案沒有結果**
A：關鍵字過於精確或檔名不符，嘗試更短的關鍵字

**Q：下載連結無法開啟**
A：api_server.py 未啟動，或 Cloudflare Tunnel URL 已過期需重新建立

**Q：匯入 .xls 失敗**
A：先轉換成 .xlsx 格式再匯入

**Q：匯入 JPG 失敗**
A：需安裝 Tesseract OCR，執行 `winget install UB-Mannheim.TesseractOCR`，
安裝後將 `C:\Program Files\Tesseract-OCR` 加入系統 PATH

---

## 七、重要元件版本

| 元件 | 版本 / 位置 |
|------|------------|
| n8n | 2.12.3（Docker, port 5681）|
| PostgreSQL + pgvector | Docker container `pg_container` |
| Ollama LLM | qwen2.5（port 11434）|
| Ollama Embedding | bge-m3（1024 維）|
| Python API Server | scripts/api_server.py（port 8765）|
| LINE Webhook | n8n 工作流 `LINE QA Assistant`（ID: ANf96ECpcq8gTuPC）|
